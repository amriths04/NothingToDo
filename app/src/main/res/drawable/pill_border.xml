<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 🔴 Selected state: red background + light border -->
    <item android:state_selected="true">
        <shape android:shape="rectangle">
            <solid android:color="#33F44336" /> <!-- translucent red -->
            <stroke android:width="1dp" android:color="#F44336" />
            <corners android:radius="50dp" />
        </shape>
    </item>

    <!-- 🟪 Default: transparent + neutral border -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <stroke android:width="1dp" android:color="?attr/cardTextSecondary" />
            <corners android:radius="50dp" />
        </shape>
    </item>

</selector>
