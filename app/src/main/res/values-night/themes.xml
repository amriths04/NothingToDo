<!-- res/values-night/themes.xml -->
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.NothingTasks" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="cardBackgroundColor">@color/background</item>      <!-- dark background -->
        <item name="cardTextPrimary">@color/textPrimary</item>         <!-- white text -->
        <item name="cardTextSecondary">@color/textSecondary</item>     <!-- lighter grey -->
        <item name="cardIconTint">@color/flagRed</item>
        <item name="secbg">@color/secbackground</item>
    </style>
</resources>

